## Changes

- Fixed OpenClaw legacy-plugin migration so a disabled legacy registration no longer disables telemetry in the built-in adapter.
- Preserved the legacy runtime `enabled` setting as the source of truth while the installer disables the superseded plugin registration.
- Retained all v0.1.24-rc7 OpenClaw GTrace AI semantic-convention alignment and CodeBuddy JSONL compatibility.

## Validation

Full Go tests, vet, release builds, pull-request CI, six-platform package smoke tests, and Windows Hook smoke tests passed.

Regression coverage verifies that reinstalling the built-in OpenClaw adapter after the legacy registration has been disabled keeps telemetry enabled and creates Hook diagnostics on invocation.
