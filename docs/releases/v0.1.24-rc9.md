## Changes

- Fixed missing OpenClaw LLM spans when typed model hooks are unavailable but the terminal transcript contains one attributable assistant model response.
- Recovered the LLM window from observed turn and assistant timestamps without fabricating a 1 ms duration.
- Added standard GenAI input/output messages, provider/model identity, finish reason, and per-message token usage to the recovered LLM span.
- Added content-free Hook observation counts to OpenClaw diagnostics for future lifecycle troubleshooting.
- Retained the v0.1.24-rc8 legacy migration fix and all v0.1.24-rc7 semantic-convention alignment.

## Validation

Full Go tests, vet, release builds, Node bridge tests, pull-request CI, six-platform package smoke tests, and Windows Hook smoke tests passed.

Live validation covered both the embedded OpenClaw model path and the OpenAI/Codex harness path. Regression coverage verifies transcript-backed LLM timing, standard input/output messages, token attribution, and omission when model identity or trustworthy boundaries are unavailable.
